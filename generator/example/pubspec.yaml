name: flutter_event_component_system_generator_example
description: "Example project for flutter_event_component_system_generator package"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  flutter_event_component_system:
    path: ../../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.11.1
  flutter_event_component_system_generator:
    path: ../

flutter:
  uses-material-design: true
