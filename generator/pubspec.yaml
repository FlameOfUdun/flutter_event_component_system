name: flutter_event_component_system_generator
description: Code generator for Flutter Event-Component-System. Generates ECS features, components, events, and systems.
issue_tracker: https://github.com/FlameOfUdun/flutter_event_component_system/issues
repository: https://github.com/FlameOfUdun/flutter_event_component_system
version: 1.0.0-beta.5

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  analyzer: ^10.0.1
  build: ^4.0.4
  source_gen: ^4.2.0
  dart_style: ^3.0.0
  flutter_event_component_system: ^1.0.0-beta.1

dev_dependencies:
  build_runner: 2.13.1
  build_test: ^3.5.7
  test: ^1.24.0
