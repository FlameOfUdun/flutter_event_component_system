name: flutter_ecs_example
description: "Example project"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  shared_preferences: ^2.5.3

  flutter_event_component_system:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  build_runner: 2.13.1

  flutter_event_component_system_generator:
    path: ../generator

flutter:
  uses-material-design: true
